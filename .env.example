# Código de acesso ao app (tela de entrada). Copie este arquivo para ".env",
# defina o valor, e configure a mesma variável no painel da Vercel/Netlify
# em "Environment Variables" antes do deploy. Se não for definida, o app usa
# "casaemordem" como padrão.
VITE_ACCESS_CODE=escolha-um-codigo-aqui
